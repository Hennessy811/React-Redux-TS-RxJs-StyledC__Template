import React, { FC } from "react";
import { HeroWrapper } from "../../shared/components/shared.components";

const Todos: FC = () => {
    return (
        <HeroWrapper>
            Todos
            {/* <List /> */}
        </HeroWrapper>
    );
};

export default Todos;
